# MoMusic 登录界面双面板布局重构任务书

> 直接将本文档完整复制给 Cursor / Claude / GPT 等 AI 代码助手即可开始重构。

---

## 一、任务目标

将 MoMusic 音乐播放器现有的**节点连线式登录模态框**重构为**左右分栏双面板布局**，同时保留所有原有业务功能和JavaScript逻辑。

---

## 二、项目位置

工作目录：`MoMusic Android APK/`

需要修改的核心文件：
1. **HTML结构**: `www/index.html` 中 id 为 `login-modal` 的模态框区域（约第1543-1650行）
2. **样式文件**: `www/css/index.css` 中所有 `.dual-login-modal`、`.login-node-graph`、`.login-auth-drawer` 相关样式
3. **打包同步**: 修改完 `www/` 后必须同步到 `android/app/src/main/assets/public/` 对应目录

---

## 三、现有结构分析

### 当前布局（节点连线式）
```
┌─────────────────────────────────────────────────────┐
│  [登录接入]                    [退出登录] [×]       │
├─────────────────────────────────────────────────────┤
│  ┌─────────┐  ┌─────────┐  ┌─────────┐             │
│  │ 网易云  │  │ QQ音乐  │  │ 酷狗    │   ╭───╮     │
│  │  ●──────┼──┼─────────┼──┼─────────┼──►│MR │     │
│  └─────────┘  └─────────┘  └─────────┘   │   │     │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐   │QR │     │
│  │ 汽水    │  │Spotify  │  │ 红尘客栈│   │CK │     │
│  └─────────┘  └─────────┘  └─────────┘   ╰───╯     │
│                    ← 连线拖拽区 →                   │
├─────────────────────────────────────────────────────┤
│  右侧抽屉: 标题 + 二维码/Cookie面板 + 操作按钮      │
└─────────────────────────────────────────────────────┘
```

### 目标布局（双面板）
```
┌──────────────────────────────────────────────────────────────────┐
│                        MoMusic 登录                        [×]   │
├───────────────────────────────┬──────────────────────────────────┤
│                               │                                  │
│     左侧品牌展示区 (55%)       │      右侧登录功能区 (45%)         │
│                               │                                  │
│  ┌──────┐                     │  ┌────────────────────────────┐  │
│  │ ICON │  MoMusic            │  │ 欢迎回来                   │  │
│  └──────┘                     │  │ 选择音乐平台开始聆听       │  │
│                               │  └────────────────────────────┘  │
│  多平台聚合音乐               │                                  │
│  一站式聆听体验               │  ┌────┐ ┌────┐ ┌────┐           │
│                               │  │网易│ │QQ  │ │酷狗│           │
│  ┌─────────────────────────┐  │  └────┘ └────┘ └────┘           │
│  │ ✓ 6大平台聚合           │  │  ┌────┐ ┌────┐ ┌────┐           │
│  │ ✓ 本地安全 数据不上传    │  │  │汽水│ │SP  │ │客栈│           │
│  │ ✓ 极速同步              │  │  └────┘ └────┘ └────┘           │
│  └─────────────────────────┘  │                                  │
│                               │  ┌────────────────────────────┐  │
│   动态音频可视化背景          │  │ [扫码] [Cookie]  ← Tab切换  │  │
│   (Canvas粒子+音波)           │  │                            │  │
│                               │  │     二维码 / Cookie输入    │  │
│                               │  │                            │  │
│                               │  └────────────────────────────┘  │
│                               │                                  │
│                               │  [先随便听听]    [开始登录 ▶]    │
│                               │                                  │
└───────────────────────────────┴──────────────────────────────────┘
```

---

## 四、必须保留的功能（ID不能改！）

### HTML 元素 ID 清单（必须100%保留，不能改名）
```javascript
// 容器
"login-modal"               // 模态框遮罩层
"login-node-graph"          // 节点图容器（可改为新布局容器但ID保留）
"login-auth-drawer"         // 登录详情面板
"login-platform-tabs"       // 平台选择按钮容器

// 平台按钮
"login-provider-netease"    // 网易云
"login-provider-qq"         // QQ音乐
"login-provider-kugou"      // 酷狗
"login-provider-qishui"     // 汽水音乐
"login-provider-spotify"    // Spotify
"login-red-dust-inn-btn"    // 红尘客栈

// 登录模式
"login-mode-official"       // 扫码/官方登录
"login-mode-cookie"         // Cookie导入

// 二维码相关
"qr-shell"                  // 二维码容器
"qr-img"                    // 二维码图片
"qq-web-login-card"         // 官方窗口登录按钮
"qr-status"                 // 状态文字

// Cookie面板
"qq-cookie-panel"           // Cookie面板容器
"qq-cookie-input"           // Cookie输入框
"qq-cookie-save-btn"        // 保存按钮
"qq-cookie-toggle-btn"      // 切换Cookie面板按钮

// 操作按钮
"refresh-qr-btn"            // 主按钮（开始登录/刷新）
"login-reset-all-btn"       // 退出所有账号

// 标题文字
"login-modal-title"         // 登录标题
"login-modal-desc"          // 登录说明文字

// 彩蛋相关（不要删除）
"login-easter-egg-gate"     // 彩蛋门
"login-easter-eye-trigger"  // 大小眼触发
// ...其他easter-egg相关元素全部保留，只是默认隐藏即可
```

### 必须保留的 JavaScript 函数（不要修改）
这些函数在 `www/js/modules/08-account/` 下，**只需要调用，不需要修改逻辑**：
```javascript
selectLoginProviderNode(provider)  // 选择平台: 'netease'|'qq'|'kugou'|'qishui'|'spotify'
selectLoginMode(mode)              // 切换模式: 'official'|'cookie'
openProviderWebLogin()             // 打开官方窗口登录
submitQQCookieLogin()              // 提交Cookie
toggleQQCookiePanel()              // 切换Cookie面板显示
startSelectedLoginConnection()     // 开始登录（主按钮点击）
closeLoginModal()                  // 关闭模态框
logoutAllAccountsAndResetEasterEgg() // 退出所有账号
toggleRedDustInnFromLogin()        // 红尘客栈开关
```

---

## 五、设计规范

### 配色系统（冰蓝色主题，匹配应用图标）
```css
:root {
  /* 品牌主色 - 冰蓝/天青 */
  --brand-primary: #38bdf8;
  --brand-light: #7dd3fc;
  --brand-lighter: #bae6fd;
  --brand-dark: #0ea5e9;
  --brand-darker: #0284c7;
  --brand-gradient: linear-gradient(135deg, #0ea5e9 0%, #38bdf8 40%, #22d3ee 80%, #06b6d4 100%);
  --brand-glow: rgba(56, 189, 248, 0.45);

  /* 背景 - 深蓝夜空 */
  --bg-base: #050914;
  --bg-card: rgba(12, 22, 38, 0.72);
  --bg-glass: rgba(255, 255, 255, 0.035);
  --bg-glass-hover: rgba(255, 255, 255, 0.065);

  /* 文字 */
  --text-primary: #f0f9ff;
  --text-secondary: rgba(240, 249, 255, 0.65);
  --text-muted: rgba(240, 249, 255, 0.40);

  /* 边框 */
  --border-subtle: rgba(255, 255, 255, 0.08);
  --border-bright: rgba(56, 189, 248, 0.35);

  /* 平台品牌色 */
  --platform-ne: #f05454;   /* 网易云红 */
  --platform-qq: #31c27c;   /* QQ绿 */
  --platform-kg: #2ec4f0;   /* 酷狗蓝 */
  --platform-qs: #00c9a7;   /* 汽水青 */
  --platform-sp: #1db954;   /* Spotify绿 */
  --platform-rdi: #f0c040;  /* 客栈金 */

  /* 圆角 */
  --radius-sm: 10px;
  --radius-md: 14px;
  --radius-lg: 18px;
  --radius-xl: 24px;

  /* 动效 */
  --ease-out: cubic-bezier(0.16, 1, 0.3, 1);
  --ease-spring: cubic-bezier(0.34, 1.56, 0.64, 1);
}
```

### 左侧品牌区设计要点
1. **背景**: 深蓝渐变 + Canvas 音频可视化粒子效果（64条频谱柱+浮动光点）
2. **Logo**: 使用 `build/icon.png`（二次元银发蓝瞳少女图标），72x72px，圆角18px，带蓝色发光
3. **品牌名**: "MoMusic" 大标题，40px 粗体，蓝白渐变文字
4. **Slogan**: "多平台聚合音乐 · 一站式聆听体验"
5. **特色卡片**: 三个卖点（平台聚合/本地安全/极速同步），带图标
6. **装饰**: 浮动音符、底部动态音波条

### 右侧功能区设计要点
1. **欢迎标题**: "欢迎回来" + 副标题 "选择音乐平台开始你的聆听之旅"
2. **平台选择**: 2行3列网格卡片（不再是连线节点）
   - 每个卡片包含: 平台色块Logo + 名称 + 小字说明
   - 选中态: 品牌色边框发光 + 右上角对勾
   - Hover: 上浮2px + 微发光
3. **登录方式Tab**: shadcn 风格 Tabs
   - "扫码登录" / "Cookie导入" 两个选项
   - 滑块指示器带弹性动画
   - 扫码选中时显示二维码区域
   - Cookie选中时显示输入区域
4. **二维码区域**:
   - 四角L形装饰（蓝色呼吸动画）
   - 扫描线上下循环动画
   - 状态文字（等待扫码/已扫描/已过期）
   - 过期显示刷新按钮
5. **Cookie输入区**:
   - 多行文本域，代码编辑器风格
   - 右上角"粘贴"按钮
   - 帮助提示文字
6. **底部按钮**:
   - 左侧: "先随便听听"（游客模式，文字按钮）
   - 右侧: "开始登录"（主按钮，蓝渐变+发光+箭头图标）
   - 右上角保留×关闭按钮

### 交互动效要求
1. **模态框入场**: 从透明度0+缩放0.98到1，时长400ms，使用expo.out缓动
2. **面板滑入**: 左侧从左滑入，右侧从右滑入，错开100ms
3. **平台选择**: 点击时品牌色边框扩散动画
4. **Tab切换**: 滑块弹性移动，内容淡入淡出
5. **按钮悬停**: 上浮1-2px + 发光增强 + 光泽扫过效果
6. **主按钮点击**: 波纹效果
7. **支持 `prefers-reduced-motion`** 无障碍设置

---

## 六、实现步骤建议

### Step 1: 替换HTML结构（www/index.html）
1. 保留 `<div id="login-modal" class="modal-mask">` 外层
2. 保留彩蛋相关元素（easter-egg）放在最底层默认隐藏
3. 将 `dual-login-modal` 内部重构为双栏flex布局
4. 所有原有功能ID必须保留，只是位置和class改变
5. 红尘客栈设置弹窗（`red-dust-inn-settings`、`rdi-category-popover`）保留在功能区内

### Step 2: 重写CSS样式（www/css/index.css）
1. 在CSS文件末尾（约18680行后）追加新样式，不要删除原有样式（避免影响其他模态框）
2. 使用更高优先级选择器覆盖旧样式：`#login-modal .new-class`
3. 旧的 `.login-node-graph` 样式可以保留但用新布局覆盖
4. 工作流连线SVG（`#login-workflow-svg`）直接 `display: none` 隐藏
5. MR节点、flow-port等连线相关元素隐藏不需要

### Step 3: Canvas粒子背景
在左侧品牌区添加一个canvas元素，实现音频可视化风格的动态背景：
- 64条频谱柱状条（随机高度，缓慢律动）
- 连线粒子网络（30-50个粒子，近距连线）
- 浮动光点效果
- 蓝色主色调

### Step 4: 适配JavaScript（只加不改）
在页面底部或单独script标签中添加：
1. 平台按钮点击：调用原有 `selectLoginProviderNode()`，同时切换选中样式
2. Tab切换：控制二维码/Cookie面板显示，调用 `selectLoginMode()`
3. 二维码刷新：调用原有 `refreshQr()` 或 `startSelectedLoginConnection()`
4. Canvas粒子动画初始化
5. **不要修改** `js/modules/08-account/` 下的任何原有逻辑文件

### Step 5: 同步到android打包目录
```bash
# Windows PowerShell
Copy-Item www/css/index.css android/app/src/main/assets/public/css/ -Force
Copy-Item www/index.html android/app/src/main/assets/public/ -Force
Copy-Item www/build/icon.png android/app/src/main/assets/public/build/ -Force
```

---

## 七、参考设计文件

设计预览稿（可作为视觉参考）：
`momusic-login-redesign-draft.design/pages/login.html`

这个文件是完整的双面板设计演示版，包含所有动画效果，可以直接打开在浏览器中查看效果作为参考，但注意这是独立演示文件，不能直接替换业务代码。

---

## 八、验收标准

1. [ ] 打开登录模态框显示双面板布局，无JS报错
2. [ ] 6个平台按钮都可以点击切换，点击后正确高亮
3. [ ] 扫码/Cookie Tab切换正常，二维码和Cookie面板正确显示切换
4. [ ] 点击"开始登录"按钮能正常触发登录流程（弹出官方窗口/二维码刷新）
5. [ ] Cookie输入后点击保存正常提交
6. [ ] 关闭按钮、游客模式按钮功能正常
7. [ ] 红尘客栈按钮点击正常弹出设置
8. [ ] 响应式：窗口缩小时（<900px）自动切换为单栏布局（品牌区在上，功能区在下）
9. [ ] 所有动画流畅，无卡顿
10. [ ] 同步到android/assets目录后打包APK，安装后登录界面显示新设计

---

## 九、注意事项

1. **不要删除原有彩蛋功能**（大小眼解锁），只是在正常登录状态下隐藏即可，彩蛋逻辑会自动处理显示
2. **不要修改任何JS模块文件**，所有交互都通过调用现有全局函数实现
3. 保留 `gsap` 动画库的使用（模态框开关已经用gsap处理）
4. 图标使用 Lucide 图标库，已经全局加载，可以直接用 `<i data-lucide="xxx"></i>` 然后调用 `lucide.createIcons()` 初始化
5. 样式追加在CSS末尾即可，不需要删除旧样式，用选择器优先级覆盖
6. 平台选中状态由原JS自动添加 `.active` class，只需写好 `.active` 样式

---

## 十、快速开始

你现在就可以按照这个结构开始重构。建议先完成HTML结构替换，再写CSS样式，最后加Canvas和微调动画。每完成一部分可以在浏览器中打开 `www/index.html` 预览效果。

如果遇到不清楚原有逻辑的地方，优先查看 `www/js/modules/08-account/03-login-modal-flows.js` 中的 `updateLoginProviderUi()` 函数，它控制了所有UI状态更新。
